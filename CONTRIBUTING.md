# 如何参与 (How to Contribute)

这个仓库收录面向养老、儿童友好、动物福利场景的智能空间**解决方案**。参与方式只有两条路径。

## 路径一：提案新方案

你有一个想法或已经在做的方案，想收录进来：

1. 用 [提案新方案](../../issues/new?template=proposal.yml) Issue 模板发起
2. 在 Discussions 里跟大家讨论、打磨
3. 方案成熟后，提 PR 把它写成一篇文档放进 `docs/<领域>/<方案名>.md`，合并后即收录

## 路径二：认领需求

维护者会发布还没人做的需求（任务清单）：

1. 在 Issues 里筛选 `type:need` + `status:open` 的条目
2. 评论认领，维护者会把 Issue 指派给你（`status` 改成 `in-progress`）
3. 做完后提 PR 关联对应 Issue，合并后 `status` 改成 `done`

认领后长期没进展怎么办？目前靠人工 triage，维护者会主动跟进，暂不做自动释放。

## Labels 说明

- `type:proposal` / `type:need` — 区分是社区提案还是官方需求
- `area:elder-care` / `area:child-friendly` / `area:animal-welfare` — 所属领域
- `status:open` / `status:in-progress` / `status:done` — 进度，两条路径共用同一套状态

## 其他

- 有想法但还不成形？先去 Discussions 聊，不用急着开 Issue
- 找不到合适的领域分类？在提案里说明，维护者会帮忙判断
